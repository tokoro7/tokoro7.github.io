{
  "name": "tokoro7-github-io",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run src/build.ts",
    "dev": "bun run --watch src/build.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
